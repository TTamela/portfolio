// Smooth scrolling and simple fade-in animations
document.addEventListener('DOMContentLoaded', () => {
    console.log("Portfolio Loaded");
    
    // You can add logic here to dynamically load projects from a JSON file later!
});